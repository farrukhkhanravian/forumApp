<%= form_with model: @like do |form| %>

  <div class="row">
    <div class="form-group col-md-4">
      <%= form.hidden_field :@like[product.id, current_user.id] %>
    </div>
  </div>
  <div class="actions">
    <%= form.submit "Like",  class: "btn btn-primary" %>
  </div>
<% end %>
